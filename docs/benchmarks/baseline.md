# Baseline Benchmarks

## Translation
- verifies start codon behavior
- verifies invalid base rejection
- verifies stop codon stopping

## Spectrum Matching
- tolerance = 0.05
- greedy one-to-one matching
- score = matches / len(theoretical)
